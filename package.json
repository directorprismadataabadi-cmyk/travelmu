{
  "name": "travelmu",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "cp .env.dev .env && nuxt dev",
    "build:dev": "cp .env.dev .env && nuxt build",
    "build:staging": "cp .env.staging .env && nuxt build",
    "build:prod": "cp .env.prod .env && nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.7.2",
    "@pinia/nuxt": "^0.11.3",
    "@tailwindcss/vite": "^4.1.18",
    "@vueuse/nuxt": "^13.9.0",
    "jwt-decode": "^4.0.0",
    "nuxt": "^4.2.2",
    "nuxt-security": "^2.5.0",
    "tailwindcss": "^4.1.18",
    "vue": "^3.5.26"
  },
  "devDependencies": {
    "@nuxtjs/i18n": "^10.2.1",
    "@nuxtjs/tailwindcss": "^6.14.0"
  }
}